{
  "name": "@provisioner/native",
  "version": "0.1.0",
  "private": true,
  "description": "Native (Tauri) flavor: bundles the web SPA in a desktop shell and provides USB/serial over a Rust backend.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
